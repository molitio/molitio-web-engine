{
    "rules": {
        "at-rule-no-unknown": [
            true,
            {
                "ignoreAtRules": ["tailwind", "apply", "variants", "screen", "layer", "theme", "config", "responsive"]
            }
        ]
    }
}

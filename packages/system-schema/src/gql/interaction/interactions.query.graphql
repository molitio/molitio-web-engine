type Query {
    interactions: [Interaction]
}

query getInteractions {
    interactions {
        id
        app {
            id
            name
        }
        interactionType
        responseEmail
        message
    }
}

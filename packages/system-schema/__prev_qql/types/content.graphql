type App {
    id: ID!
    name: String!
    owner: Human!
    description: String
    url: String
    organization: Organization
}

type AppContent {
    id: ID!
    app: App!
    pages: [Page]
}

type Page {
    id: ID!
    name: String!
}

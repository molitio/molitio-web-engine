//import { startViewServer } from './server/server';

const context = {
    uiPort: 3000,
};

/* const server = startViewServer();

server.listen(context.uiPort, () => {
    console.log(`Server running on /:${context.uiPort}`);
});
 */

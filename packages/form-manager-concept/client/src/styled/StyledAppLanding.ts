import styled from "styled-components";
import { StyledPage } from "./StyledGlobal";

export const StyledAppLanding = styled(StyledPage)``;

export const StyledAppLandingTitle = styled.h2`
  color: var(--text-primary);
`;

export const StyledAppLandingInfoText = styled.p``;

import styled from 'styled-components';

export const ResourceGalleryContainer = styled.div`
    flex: 1;
    display: flex;
    flex-direction: row;
`;

export const ResourceGalleryResourcePreview = styled.div`
    flex: 1;
`;

export const ResourceGalleryTitle = styled.h2`
    text-align: center;
`;

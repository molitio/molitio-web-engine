import { ThemeColor } from '../../ui-style-service';
export type TypographyProps = {
    rem: number;
    color?: ThemeColor;
    marginTop?: string;
    maxContent?: boolean;
    center?: boolean;
    textContent?: string;
};

import styled from "styled-components";
import { StyledPage } from "./StyledGlobal";

export const StyledSurveyView = styled(StyledPage)`
  justify-content: start;
  padding: 3em 1em 1em 1em;
  grid-column: 1 / span 2;
`;

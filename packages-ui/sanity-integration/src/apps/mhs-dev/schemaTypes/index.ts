import { aboutPageType } from './aboutPageType';
import { landingPageType } from './landingPageType';
import { contextNode } from '../../../common/schemaTypes/contextNode';
import { appContext } from '../../../common/schemaTypes/appContext';
import { rootNode } from '../../../common/schemaTypes/rootNode';

export const schemaTypes = [aboutPageType, landingPageType, contextNode, appContext, rootNode];

/* import { styled } from '@mui/system';

import { ThemeColorVariant } from '../../system-theme';

export const StyledCoverPage = styled('div')`
    position: relative;
    height: 100%;
`;

export const StyledCoverPageBackground = styled('div')<{ $background_colorvariant?: ThemeColorVariant }>`
    position: relative;
    background-color: ${(props) => props.theme?.palette?.background?.[props?.$background_colorvariant || 'default']};
    height: 100%;
`;

export const StyledCoverPageTitle = styled('h1')<{ $colorvariant: ThemeColorVariant }>`
    color: ${(props) => props.theme?.palette?.text?.[props?.$colorvariant || 'main']};
`;

export const StyledCoverContent = styled('div')`
    border: 3px dashed purple;
    position: absolute;
    top: 200px;
    left: 150px;
    width: 200px;
    height: 100px;
`;

export const StyleCoverPageContentText = styled('span')<{ $colorvariant: ThemeColorVariant }>`
    color: ${(props) => props.theme?.palette?.text?.[props?.$colorvariant || 'main']};
`;

export const StyleCoverPageContentCta = styled('div')``;
 */
